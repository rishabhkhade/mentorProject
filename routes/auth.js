const express = require('express')
const router = express.Router()

const login = require('../controllers/employee/auth/login')


// router.post('/register', login.register)
// router.post('/login', login.login)

module.exports = router
